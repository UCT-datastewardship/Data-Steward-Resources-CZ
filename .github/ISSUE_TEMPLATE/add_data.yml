name: 📝 Add New Entry to Database
description: Use this form to add a row to data/data.csv
title: " Data Entry " # This pre-fills the box with just " Data Entry "
labels: ["data-entry"]
body:
  - type: markdown
    attributes:
      value: |
        ### ⚠️ Note for "Add a title" above:
        Please do not change the **" Data Entry "** text in the title box.
  - type: markdown
    attributes:
      value: "### 📝 Resource Details"
  - type: input
    id: title
    attributes:
      label: Title
      placeholder: "e.g., Data Management Plan Template"
    validations:
      required: true
  - type: input
    id: link
    attributes:
      label: Link to source
# ... (rest of your fields: categories, knowledge_level, duration_choice, etc.)
